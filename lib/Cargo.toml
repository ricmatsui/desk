[package]
name = "lib"
build = "build.rs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "dylib"]

[dependencies]
raylib = { version = "3.7" }
rppal = { version = "0.14.1", optional = true }

[features]
default = []
reloader = []
pi = ["dep:rppal"]
